name: CI email-service

on:
  push:
    branches: [main]
    paths: ["backend/email-service/**"]
  pull_request:
    branches: [main]
    paths: ["backend/email-service/**"]
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend/email-service
    env:
      DATABASE_URL: postgresql://dummy:dummy@localhost:5432/dummy_db
      AMQP_URL: amqp://dummy:dummy@localhost:5672/

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install uv
        run: pip install uv

      - name: Install dependencies
        run: uv sync

      - name: Ruff format check
        run: uv run ruff format --check

      - name: Ruff lint
        run: uv run ruff check

      - name: Run pytest
        run: uv run pytest --cov=src --cov-report=xml --cov-branch

      - name: Upload coverage
        if: hashFiles('coverage.xml') != ''
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          flags: email-service
          fail_ci_if_error: false
